#include <iostream>
#include "PartnerAccount.h"

PartnerAccount::PartnerAccount(int number, Client* c, Client* p) : Account(n, o)
{
	this->partner - p;
}
PartnerAccount::PartnerAccount(int number, Client* c, Client* p, double ir) : Account(n, o)
{
	this->partner = p;
}
